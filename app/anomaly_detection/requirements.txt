# =============================================================================
# app/anomaly_detection/requirements.txt
#
# Dependencies for the AIOps infrastructure anomaly detection service
# (app.anomaly_detection.detector:app). Separate from app/serving/ — this
# service does not use MLflow, and it does need the elasticsearch client.
# =============================================================================

fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4

numpy==2.4.6
pandas==2.3.3
scikit-learn==1.8.0

prometheus_client==0.25.0
requests==2.34.2
elasticsearch==8.12.0
